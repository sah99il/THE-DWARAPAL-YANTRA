dataset:
  root_dir: data/identity
  image_size: 224

training:
  batch_size: 32
  num_epochs: 10
  learning_rate: 0.0001
  weight_decay: 0.0005

model:
  embedding_dim: 512
  backbone: vit_base_patch16_224

arcface:
  scale: 64.0
  margin: 0.5

checkpoint:
  save_dir: models/checkpoints/identity
